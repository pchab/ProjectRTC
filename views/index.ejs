<%= include layoutTop %>
  
  	<h1><%= header %></h1>
  	
  	<%= include localCam %>

	<%= include remoteStreams %>

	<script>
		client = new PeerManager();
		vm = new RTCViewModel(client, "/streams/<%- id %>");
		vm.refresh();
		ko.applyBindings(vm);

		<% if(!!id) { %>
			vm.toggleLocalVideo();
		<% } %>
	</script>
	
<%= include layoutBottom %>